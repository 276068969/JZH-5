services:
  migratory-bird-platform:
    build: .
    container_name: migratory-bird-platform
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
